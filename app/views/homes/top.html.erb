<%= render partial: 'recipes/header'%>

<div class = "container">
  <div class = "main-wrapper">
    <div class = "new">NEW!!</div>
      <% @recipes.each do |recipe| %>
        <%= link_to recipe_path(recipe.id) do %>
          <div class = "recipe-new content">
            <h1 class="recipe-title-new"><%= recipe.title %></h1>
            <div class = "recipe-image-type-new">
              <%= image_tag recipe.image.variant(resize:'600x600').processed , class: "recipe-image" if recipe.image.attached? %>
              <p><%= recipe.recipe_type.name %></p>
            </div>
            <% recipe.tags.each do |tag| %>
              <span class= 'tag-name'><%= tag.name %></span>
            <% end %>
            <br>
            <div class = "recipe-subtitle-new">
              <%= recipe.subtitle %>   
            </div>
          </div>
        <% end %>
      <% end %>
  </div>

  <div class = "sidebar">
    <%= render partial: 'recipes/sidebar'%>
  </div>
</div>

